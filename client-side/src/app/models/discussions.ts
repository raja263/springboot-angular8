import {User} from './user';
import DateTimeFormat = Intl.DateTimeFormat;
export class Discussions {
  discussionID: number;
  title: string="";
  body: string="";
  timestamp: number;
  userID: number;

  //instructor: User;
}
